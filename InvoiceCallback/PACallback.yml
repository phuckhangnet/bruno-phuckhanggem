info:
  name: PACallback
  type: http
  seq: 1

http:
  method: POST
  url: https://phuckhangdev.ngoanthachviet.com/api/InvoiceCallback/PACallback
  body:
    type: json
    data: |-
      {
          "data":"hau",
          "signatures":"dddd"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
