info:
  name: Tìm kiếm khách hàng màn bán hàng
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/Customer/TimKiemKhachHang"
  headers:
    - name: userIdLogin
      value: "{{userIdLogin}}"
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: |-
      {
        "searchTerm": "0967900801"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
