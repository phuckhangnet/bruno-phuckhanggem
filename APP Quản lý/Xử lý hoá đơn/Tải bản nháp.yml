info:
  name: Tải bản nháp
  type: http
  seq: 12

http:
  method: POST
  url: "{{url}}/DataManagement/GetInvoiceDownloadLink"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "1"
  body:
    type: json
    data: |-
      {
          "IdDonHang": 4387
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
