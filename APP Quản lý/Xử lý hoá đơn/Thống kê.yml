info:
  name: Thống kê
  type: http
  seq: 7

http:
  method: POST
  url: "{{url}}/DataManagement/GetOrderInvoiceStatistics"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "1"
      disabled: true
  body:
    type: json
    data: |-
      {
          "FromDate": "2026-01-01",
          "ToDate": "2026-01-01"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
