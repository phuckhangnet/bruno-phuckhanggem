info:
  name: Khóa nhân viên
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/Employee/Admin/KhoaNhanVien"
  headers:
    - name: userIdLogin
      value: "{{userIdLogin}}"
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: |-
      {
        "IdNguoiDung":51,
        "IsLock":true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
