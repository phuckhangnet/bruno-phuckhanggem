info:
  name: GetNhanVienChiTiet
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/Employee/GetNhanVienChiTiet?id=8"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  params:
    - name: id
      value: "8"
      type: query
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
