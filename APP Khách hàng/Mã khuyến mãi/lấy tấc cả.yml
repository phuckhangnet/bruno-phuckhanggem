info:
  name: lấy tấc cả
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/ma-khuyen-mai/lay-tac-ca"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
