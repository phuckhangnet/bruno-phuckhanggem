info:
  name: theo dõi hành trình đơn hàng
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/customer/don-hang/hanh-trinh-don-hang"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: |-
      {
         "idDonHang":1
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
